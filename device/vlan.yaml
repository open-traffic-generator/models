components:
  schemas:
    Device.Vlan:
      x-include: 
        - '../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        Emulated VLAN protocol
      type: object
      properties:
        tpid:
          x-device-pattern:
            description: >-
              Tag protocol identifier
            format: enum
            enums: [8100, 88A8, 9100, 9200, 9300]
            default: 8100
        priority:
          x-device-pattern:
            description: >-
              Priority code point
            format: integer
            length: 3
            default: 0
        id:
          x-device-pattern:
            description: >-
              VLAN identifier
            format: integer
            length: 12
            default: 1
    