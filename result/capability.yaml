openapi: 3.1.0

info:
  title: Capabilities results models
  version: 1.0.1

components:
  schemas:
    Result.Capabilities:
      description: >-
        TBD
      type: object
      properties:
        unsupported_apis:
          type: array
          items:
            description: >-
              A #/components/schemas/... path that is not supported.
            type: string
        implementation_specific_apis:
          type: array
          items:
            description: >-
              A #/components/schemas/... path and specific implementation 
              details regarding the path.
              Specific implementation details can be min, max limits.
            type: string

