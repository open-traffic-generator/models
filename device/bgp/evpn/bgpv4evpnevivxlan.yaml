components:
  schemas:
    BgpV4.EviVxlan:
      x-include:
      - './bgpevpnevivxlan.yaml#/components/schemas/Bgp.EvpnEviVxlan'
      description: |-
        Configuration for BGP EVPN EVI. Advertises following routes - 

        Type 3 - Inclusive Multicast Ethernet Tag Route

        Type 1 -  Ethernet Auto-discovery Route (Per EVI)

        Type 1 -  Ethernet Auto-discovery Route (Per ES)
      type: object      
      properties:
        broadcast_domains:
          description: >-
            This contains the list of Broadcast Domains to be configured per EVI.
          type: array
          items:           
            $ref: './bgpv4evpnbroadcastdomainvxlan.yaml#/components/schemas/BgpV4.BroadcastDomainVxlan'

            
        


        