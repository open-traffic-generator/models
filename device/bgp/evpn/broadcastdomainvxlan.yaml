components:
  schemas:
    Bgp.BroadcastDomainVxlan:
      x-include:      
      - './broadcastdomain.yaml#/components/schemas/Bgp.BroadcastDomain'
      description: >-
        List of BroadcastDomains per EVI
      type: object
      properties: 
        mac_ranges:
          description: >-
            List of Hosts to be configured per Broadcastdomain.
          type: array
          items:           
            $ref: './evpnmacranges.yaml#/components/schemas/Bgp.CMACRanges'

        


        