components:
  schemas:
    PerPort.Options:
      description: |-
        ****Add proper description here****
      type: object
      properties:
        port_settings:
          description: >-
            This contains all the per port settings for each protocol.   
          type: array
          items:
            $ref: './perportoptions.yaml#/components/schemas/Options.PortSettings'
          x-field-uid: 1
    Options.PortSettings:
      description: |-
          ****Add proper descriptio****
      type: object
      properties:
        port_name:
          description: >-
            The name of port for which this settings will be applied to.
          type: string
          x-constraint:
          - '/components/schemas/Port/properties/name'
          x-field-uid: 1
        protocols:
          type: array
          items:
            $ref: '#/components/schemas/Port.Protocols'
          x-field-uid: 2
    Port.Protocols:
      description: |-
        Supprted protocols.
      type: object  
      properties:      
        choice:
          description: |-
            list of protocols that have per port settings
          type: string
          default: rocev2
          x-enum:
            rocev2:
              x-field-uid: 1
          x-field-uid: 1
        rocev2:
          description: |-
            RoCEv2 Port Settings.
          type: array
          items:
            $ref: './rocev2.yaml#/components/schemas/Rocev2.PerPortSettings'
          x-field-uid: 2