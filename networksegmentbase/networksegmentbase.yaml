components:
  schemas:
    NetworkSegmentBase:
      properties:
        name:
          $ref: ../name/name.yaml#/components/schemas/Name
          x-field-uid: 1
        networktags:
          $ref: ../taglist/taglist.yaml#/components/schemas/TagList
          x-field-uid: 3
      required:
      - name
      type: object
