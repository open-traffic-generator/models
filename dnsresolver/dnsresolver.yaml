components:
  schemas:
    DNSResolver:
      properties:
        cachetimeout:
          description: The cached timeout for the DNS Resolver
          format: int32
          type: integer
          x-field-uid: 1
        enableperconnect:
          description: The enable perconnection value.
          type: boolean
          x-field-uid: 2
        links:
          items:
            $ref: ../apilink/apilink.yaml#/components/schemas/APILink
          type: array
          x-field-uid: 3
        nameservers:
          description: A list of name servers.
          items:
            $ref: ../nameserver/nameserver.yaml#/components/schemas/NameServer
          type: array
          x-field-uid: 4
      type: object
