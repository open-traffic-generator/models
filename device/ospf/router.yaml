components:
  schemas:
    Device.OspfRouter:
      x-status: under-review
      x-include:
      - '../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        A container of properties for a Ospf router
      type: object
      required: [ipv4_name]
      properties:
        ipv4_name: 
          description: >-
            The unique name of ipv4 or loopback object 
            used as the source Ip this list of Ospf peer.
          type: string
          x-constraint:
          - '/components/schemas/Device.Ipv4/properties/name'
        property_dummy:
          description: >-
            TBD
          type: string
  