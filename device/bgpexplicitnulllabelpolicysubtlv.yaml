components:
  schemas:
    Device.BgpExplicitNullLabelPolicySubTlv:
      description: >-
        Container for BGP explicit null label policy sub tlv settings.
      type: object
      properties:
        enable:
          description: >-
            TBD
          $ref: './patterns.yaml#/components/schemas/Device.Pattern'
          x-pattern:
            format: boolean
            default: false
        explicy_null_label_policy:
          description: >-
            TBD
          $ref: './patterns.yaml#/components/schemas/Device.Pattern'
          x-pattern:
            format: integer
            default: 0
