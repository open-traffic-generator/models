components:
  schemas:
    Interface.Ipv4Base:
      x-include: 
      - '../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        A base IPv4 interface
      type: object
      properties:
        address:
          description: >-
            The IPv4 address
          type: string
          format: ipv4
          default: 0.0.0.0
        prefix:
          description: >-
            The prefix of the IPv4 address
          type: integer
          minimum: 1
          maximum: 32
          default: 24

    Interface.Ipv4:
      x-include: 
      - '#/components/schemas/Interface.Ipv4Base'
      description: >-
        An IPv4 interface with gateway
      type: object
      properties:
        gateway:
          description: >-
            The IPv4 address of the gateway
          type: string
          format: ipv4
          default: 0.0.0.0
          
    # Can not derive LoopbackIpv4 from Ipv4Base as default prefix length must be 32.
    Interface.LoopbackIpv4:
      x-include: 
      - '../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        An IPv4 loopback interface
      type: object
      required: [interface_name]
      properties:
        interface_name: 
          description: >-
            The unique name of ethernet object behind which this loopback 
             interface will be created.
          type: string
          x-constraint:
          - '/components/schemas/Interface.Ethernet/properties/name'
        address:
          description: >-
            The Loopback IPv4 address
          type: string
          format: ipv4
          default: 0.0.0.0
        prefix:
          description: >-
            The prefix of the Loopback IPv4 address
          type: integer
          minimum: 1
          maximum: 32
          default: 32
        
    
    

