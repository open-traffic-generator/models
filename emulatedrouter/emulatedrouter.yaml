components:
  schemas:
    EmulatedRouter:
      properties:
        emulated_router:
          items:
            $ref: ../emulated_router/emulated_router.yaml#/components/schemas/Device.EmulatedRouter
          type: array
          x-field-uid: 1
        enabled:
          type: boolean
          x-field-uid: 2
      required:
      - enabled
