components:
  schemas:
    App.Traffic:
      description: >-
        Traffic config.
      type: object
      properties:
        client:
          $ref: './client.yaml#/components/schemas/Traffic.Client'
          x-field-uid: 1
        server:
          $ref: './server.yaml#/components/schemas/Traffic.Server'
          x-field-uid: 2
        objective:
          $ref: './objective.yaml#/components/schemas/Traffic.Objective'
          x-field-uid: 3

