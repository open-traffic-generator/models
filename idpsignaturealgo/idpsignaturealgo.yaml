components:
  schemas:
    IdPSignatureAlgo:
      type: string
      x-default: default(SHA256_SIGN)
      x-enum:
        SHA1_SIGN:
          x-field-uid: 2
        SHA256_SIGN:
          x-field-uid: 1
