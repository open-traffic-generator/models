components:
  schemas:
    Bgp.EviVxlanV4:
      x-include:
      - './evivxlan.yaml#/components/schemas/Bgp.EviVxlan'
      description: >-
        Configuration for BGP EVN EVI Ranges.
      type: object      
      properties:
        broadcast_domains:
          description: >-
            List of Broadcast Domains to be configured per EVI
          type: array
          items:           
            $ref: './broadcastdomainvxlanv4.yaml#/components/schemas/Bgp.BroadcastDomainVxlanV4'

            
        


        