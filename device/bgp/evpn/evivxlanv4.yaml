components:
  schemas:
    Bgp.EviVxlanV4:
      x-include:
      - '../../device.yaml#/components/schemas/Device.Active'
      - './evivxlan.yaml#/components/schemas/Bgp.EviVxlan'
      description: >-
        Configuration for BGP EVN EVI Ranges.
      type: object      
      properties:
        broadcast_domains:
          description: >-
            List of Broadcast Domains to be configured per EVI
          type: array
          items:           
            $ref: './broadcastdomainv4.yaml#/components/schemas/Bgp.BroadcastDomainVxlanV4'

            
        


        