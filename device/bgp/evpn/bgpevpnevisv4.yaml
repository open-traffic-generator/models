components:
  schemas:
    Bgp.EvpnEvisV4:
      description: >-
        Configuration for BGP EVN EVI Ranges
      type: object
      properties:
        choice:
          type: string
          enum: [evpn_vxlan]
          default: evpn_vxlan
        evpn_vxlan:
          description: >-
            List of evpn vxlans to be configured per Ethernet Segment                    
          $ref: './bgpevpnevivxlanv4.yaml#/components/schemas/Bgp.EvpnEviVxlanV4'
          
        