components:
  schemas:
    AgentAssignments:
      description: The agents assigned to the current test configuration
      properties:
        byid:
          description: The agents statically assigned to the current test configuration.
          items:
            $ref: ../agentassignmentdetails/agentassignmentdetails.yaml#/components/schemas/AgentAssignmentDetails
          type: array
          x-field-uid: 1
        byport:
          description: The ports assigned to the current test configuration.
          items:
            $ref: ../agentassignmentbyport/agentassignmentbyport.yaml#/components/schemas/AgentAssignmentByPort
          type: array
          x-field-uid: 2
        bytag:
          description: The tags according to which the agents are dynamically assigned.
          items:
            type: string
          type: array
          x-field-uid: 3
        links:
          items:
            $ref: ../apilink/apilink.yaml#/components/schemas/APILink
          type: array
          x-field-uid: 4
      required:
      - bytag
      type: object
