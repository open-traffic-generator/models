components:
  schemas:
    Bgp.EviVxlanV6:
      x-include:      
      - '../../device.yaml#/components/schemas/Device.Active'
      - './evivxlan.yaml#/components/schemas/Bgp.EviVxlan'
      description: >-
        Configuration for EVI VxLAN V6 Ranges.
      type: object      
      properties:
        broadcast_domains:
          description: >-
            List of Broadcast Domains to be configured per EVI
          type: array
          items:           
            $ref: './broadcastdomainv6.yaml#/components/schemas/Bgp.BroadcastDomainVxlanV6'

        
          