components:
  schemas:
    Traffic.Get:
      x-include: 
      - '../../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        A get operation such as HTTP->GET
      type: object
      required: [destination]
      properties:
        destination:
          description: >-
            The Server name/IP address
          type: string
        page:
          description: >-
            The page name to perform get opertaion.
          type: string

    Traffic.Post:
      x-include: 
      - '../../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        A Post operation such as  HTTP->POST 
      type: object
      required: [destination]
      properties:
        destination:
          description: >-
            The Server name/IP address
          type: string
        page:
          description: >-
            The page name to perform get opertaion.
          type: string