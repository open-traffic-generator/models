components:
  schemas:
    AgentAssignmentByPort:
      description: Details of an agent assignment by port
      properties:
        capturesettings:
          $ref: ../capturesettings/capturesettings.yaml#/components/schemas/CaptureSettings
          description: The capture settings of the port that is assigned.
          x-field-uid: 1
        portid:
          description: The id of the port that is assigned.
          type: string
          x-field-uid: 4
      type: object
