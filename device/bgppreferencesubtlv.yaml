components:
  schemas:
    Device.BgpPreferenceSubTlv:
      description: >-
        Container for BGP preference sub tlv settings.
      type: object
      properties:
        enable:
          type: boolean
          default: false
        preference:
          type: integer
          default: 0
