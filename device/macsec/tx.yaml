components:
  schemas:
    Macsec.Tx:
      description: >-
        A container of Tx properties of SecY.
      type: object
      properties:
        txscs:
          description: >-
            Tx secure channels.
          type: array
          items:
            $ref: './txsc.yaml#/components/schemas/Macsec.TxSc'
          x-field-uid: 1
        static_key:
          $ref: '#/components/schemas/Macsec.Tx.StaticKey'
          x-field-uid: 2
    Macsec.Tx.StaticKey:
      description: >-
        Tx setting for static key.
      type: object
      properties:
        rekey_mode:
          $ref: './statickey.yaml#/components/schemas/Macsec.StaticKey.RekeyMode'
          x-field-uid: 1
