components:
  schemas:
    Traffic:
      x-include: 
      - '../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        HTTP traffic configs such as get,post commands.
      type: object
      properties:
        get:
          description: >-
            HTTP get command
          $ref: './command.yaml#/components/schemas/Traffic.Get'
        post:
          description: >-
            HTTP get command
          $ref: './command.yaml#/components/schemas/Traffic.Post'