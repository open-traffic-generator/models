components:
  schemas:
    TimelineSegmentBase:
      properties:
        duration:
          description: 'The duration of the timeline segment (default: 600).'
          format: int64
          type: integer
          x-field-uid: 1
        id:
          $ref: ../id/id.yaml#/components/schemas/ID
          x-field-uid: 4
        segmenttype:
          $ref: ../segmenttype/segmenttype.yaml#/components/schemas/SegmentType
          description: 'The segment''s type. Must be one of: SteadySegment, StepUpSegment,
            StepDownSegment.'
          x-field-uid: 2
        warmupperiod:
          default: 0
          description: 'Deprecated. Use ObjectivesAndTimeline.WarmUp instead. The
            time that servers may need to warm up, when clients should wait (default:
            0 seconds).'
          format: int64
          type: integer
          x-field-uid: 3
      required:
      - id
      - duration
      - segmenttype
      type: object
