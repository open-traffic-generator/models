components:
  schemas:
    ErrorResponse:
      description: REST API error response.
      properties:
        message:
          description: The error message
          type: string
          x-field-uid: 1
      type: object
