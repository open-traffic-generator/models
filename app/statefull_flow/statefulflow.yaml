components:
  schemas:
    StatefulFlow:
      description: >-
        Statefull_flows configure traffic for emulated protocols which is configured in apps. 
      type: object
      properties:
        client:
          $ref: './client.yaml#/components/schemas/StatefulFlow.Client'
          x-field-uid: 1
        server:
          $ref: './server.yaml#/components/schemas/StatefulFlow.Server'
          x-field-uid: 2
        objectives:
          $ref: './objective.yaml#/components/schemas/StatefulFlow.Objective'
          x-field-uid: 3

