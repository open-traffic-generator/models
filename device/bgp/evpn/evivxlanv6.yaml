components:
  schemas:
    Bgp.EviVxlanV6:
      x-include:
      - './evivxlan.yaml#/components/schemas/Bgp.EviVxlan'
      description: >-
        Configuration for EVI VxLAN V6 Ranges.
      type: object      
      properties:
        broadcast_domains:
          description: >-
            List of Broadcast Domains to be configured per EVI
          type: array
          items:           
            $ref: './broadcastdomainvxlanv6.yaml#/components/schemas/Bgp.BroadcastDomainVxlanV6'

        
          