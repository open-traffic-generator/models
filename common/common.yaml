openapi: 3.1.0

info:
  title: Commonly used schemas
  version: 1.0.0

components:
  x-property-fragments:
    description: -> 
      Fragment to be used in properties that will be replaced by the 
      pre-processor.
    UniqueName:
      description: >-
        Unique name of an object that is the primary key for objects found in 
        arrays.
      type: string
      pattern: ^[\sa-zA-Z0-9-_()><\[\]]+$
