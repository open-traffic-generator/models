components:
  schemas:
    NetworkMeshing:
      properties:
        dstipspergroup:
          default: 1
          description: The number of destination IPs to use for each meshing group.
          format: int32
          type: integer
          x-field-uid: 1
        mappingtype:
          $ref: ../mappingtype/mappingtype.yaml#/components/schemas/MappingType
          x-field-uid: 2
        srcvlanspergroup:
          default: 1
          description: The number of source VLANs to use for each meshing group.
          format: int32
          type: integer
          x-field-uid: 3
      type: object
