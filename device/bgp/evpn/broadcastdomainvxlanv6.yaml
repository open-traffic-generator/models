components:
  schemas:
    Bgp.BroadcastDomainVxlanV6:
      x-include:
      - './broadcastdomainvxlan.yaml#/components/schemas/Bgp.BroadcastDomainVxlan'
      description: >-
        Array of BroadcastDomains per EVI
      type: object
      properties: 
        mac_ranges:
          description: >-
            List of Hosts to be configured per Broadcastdomain.
          type: array
          items:           
            $ref: './evpnmacranges.yaml#/components/schemas/Bgp.CMACRanges'