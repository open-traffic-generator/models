components:
  schemas:
    InnerIPRange:
      description: The Inner IP Range assigned to the current test configuration
      properties:
        networktags:
          $ref: ../taglist/taglist.yaml#/components/schemas/TagList
          x-field-uid: 1
      type: object
