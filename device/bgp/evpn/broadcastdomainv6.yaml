components:
  schemas:
    Bgp.BroadcastDomainVxlanV6:
      x-include:      
      - '../../device.yaml#/components/schemas/Device.Active'
      - './broadcastdomain.yaml#/components/schemas/Bgp.BroadcastDomain'
      description: >-
        Array of BroadcastDomains per EVI
      type: object
      properties: 
        mac_ranges:
          description: >-
            Array of Hosts to be configured per Broadcastdomain
          type: array
          items:           
            $ref: './evpnmacranges.yaml#/components/schemas/Bgp.CMACRanges'