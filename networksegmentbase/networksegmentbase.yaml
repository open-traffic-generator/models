components:
  schemas:
    NetworkSegmentBase:
      properties:
        name:
          $ref: ../name/name.yaml#/components/schemas/Name
          x-field-uid: 1
        networktags:
          description: A list of tags.
          items:
            type: string
          type: array
          x-field-uid: 2
      required:
      - name
      type: object
