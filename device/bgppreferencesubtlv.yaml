components:
  schemas:
    Device.BgpPreferenceSubTlv:
      description: >-
        Container for BGP preference sub tlv settings.
      type: object
      properties:
        enable:
          description: >-
            TBD
          x-pattern:
            format: boolean
            default: false
        preference:
          description: >-
            TBD
          x-pattern:
            format: integer
            default: 0
