components:
  schemas:
    Bgp.EthernetSegmentV6:
      x-include:       
      - '../../../common/common.yaml#/components/schemas/Named.Object'
      - '../../device.yaml#/components/schemas/Device.Active'
      - './ethernetsegment.yaml#/components/schemas/Bgp.EthernetSegment'
      description: >-
        Configuration for BGP Ethernet Segment ranges.
      type: object
      properties:        
        df_election:
          description: >-
            Desiginated Forwarder election configuration.
          $ref: './ethernetsegment.yaml#/components/schemas/Bgp.EthernetSegment.DfElection'
        evis:
          description: >-
            Evpn Instances (EVI) to be configured per EthernetSegment.
          type: array
          items:           
            $ref: './evivxlanv6.yaml#/components/schemas/Bgp.EviVxlanV6'
        