components:
  schemas:
    NetworkMapping:
      properties:
        clientnetworktags:
          description: 'A list of tags of Network Segments which serve as clients.
            (default: Client)'
          items:
            type: string
          type: array
          x-field-uid: 1
        excludeddutlist:
          description: A list of DUTs that are excluded from client-server network
            connections.
          items:
            type: string
          type: array
          x-field-uid: 2
        servernetworktags:
          description: 'A list of tags of Network Segments which serve as servers.
            (default: Server)'
          items:
            type: string
          type: array
          x-field-uid: 3
      required:
      - clientnetworktags
      - servernetworktags
      type: object
