components:
  schemas:
    MappingType:
      description: 'Use this property to switch between different meshing types (default:
        FULL_MESH).'
      type: string
      x-enum:
        v10:
          x-field-uid: 1
        v20:
          x-field-uid: 2
