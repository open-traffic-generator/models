components:
  schemas:
    Router.OspfSimulatedTopology.Link:
      x-status: under-review
      x-include:
      - '../common/common.yaml#/components/schemas/Named.Object'
      description: >-
        This contains the 
      type: object
      properties:
        te_enabled:
          description:
            If enable is true Traffic Engineering properties will be advertised for this ospf interface.
          type: boolean
          default: false