components:
  schemas:
    BgpV4.EviVxlan.BroadcastDomain:
      x-include:      
      - './bgpevpnbroadcastdomainvxlan.yaml#/components/schemas/Bgp.EvpnBroadcastDomainVxlan'
      description: >-
        Configuration for BroadcastDomains per EVI.
      type: object
      properties: 
        cmac_ip_ranges:
          description: |-
            This contains the list of MAC/IP Ranges to be configured per Broadcastdomain. 
            
            Advertises following route - 
            Type 2 - MAC/IP Advertisement Route.
          type: array
          items:           
            $ref: './bgpevpncmacranges.yaml#/components/schemas/Bgp.CMacIpRanges'

        


        