components:
  schemas:
    InnerDevice.Ipv4:
      description: The Inner IP Range assigned to the current test configuration
      properties:
        networktags:
          description: A list of tags.
          items:
            type: string
          type: array
          x-field-uid: 1
      type: object
