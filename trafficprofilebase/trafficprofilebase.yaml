components:
  schemas:
    TrafficProfileBase:
      properties:
        active:
          default: true
          description: Indicates whether the profile is enabled or not.
          type: boolean
          x-field-uid: 1
        trafficsettings:
          $ref: ../trafficsettings/trafficsettings.yaml#/components/schemas/TrafficSettings
          x-field-uid: 2
      type: object
