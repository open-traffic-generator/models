components:
  schemas:
    Metric.Event: 
      description: |-
        A container for an event that has occurred in the system.
      type: object
      properties:
        type:
          description: |-
            The type of event that occurred.
          type: string
          enum:
          - link_down
          - link_up
          - route_withdraw
          - route_advertise
          - flow_rx_rate_above_threshold
          - flow_rx_rate_below_threshold
          example: route_withdraw
        source:
          description: |-
            The source of the event. 
            The source MUST be the value of one of the x-constraint paths,
            which means the source MUST be a unique name in the configuration. 
          type: string
          x-constraint:
          - '/components/schemas/Port/properties/name'
          - '/components/schemas/Flow/properties/name'
          - '/components/schemas/Router.Bgpv4Route/properties/name'
          - '/components/schemas/Router.Bgpv6Route/properties/name'
          example: Bgpv4 Route Range 1
        begin_timestamp_ns:
          description: |-
            The time in nanoseconds at which the event began
          type: number
          format: double
        end_timestamp_ns:
          description: |-
            The time in nanoseconds at which the event ended
          type: number
          format: double