components:
  schemas:
    App.HttpServer:
      description: >-
        Configurations of one or more HTTP server.
      type: object
      properties:
        server:
          $ref: './server.yaml#/components/schemas/HttpServer.Server'
          x-field-uid: 1