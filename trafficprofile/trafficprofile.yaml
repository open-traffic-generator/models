components:
  schemas:
    Trafficprofile:
      description: >-
        A container for L47 traffic configurations.
      type: object
      properties:
        app:
          description: >-
            application protocol config name on which traffic be applied.
          type: array
          items:
            type: string
          x-field-uid: 1
        objectives:
          description: >-
            objective configurations.
          $ref: './objective.yaml#/components/schemas/Trafficprofile.Objective'
          x-field-uid: 2