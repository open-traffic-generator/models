components:
  schemas:
    Router.IsisRouter.Advanced:
      description: |-
        This contains Isis router advanced properties.
      type: object
      properties:
        enable_hello_padding:
          description: |-
            It turns on a padded Hello message.
          type: boolean
          default: true
        # maximum_area_adress:
        #   description: |-
        #     Number of Area Addresses permitted for this ISIS layer 2 to 3 areas.
        #   type: integer
        #   default: 3
        # area_addresses:
        #   desciption: |-
        #     Combination of the ISP and HO-DSP. 
        #     Usually all nodes within an area have the same area address. 
        #     Multiple addresses can be assigned.
        #   type: string
        #   format: hex
        #   default: 00490001


