components:
  schemas:
    Mka.Tx:
      description: >-
        A container of Tx properties.
      type: object
      required: [secure_channels]
      properties:
        secure_channels:
          description: >-
            Tx secure channels.
          $ref: './txsc.yaml#/components/schemas/Mka.TxSc'
          x-field-uid: 1
