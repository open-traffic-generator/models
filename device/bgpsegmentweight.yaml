components:
  schemas:
    Device.BgpSegmentWeight:
      x-status: under-review
      description: >-
        Optional container for BGP SR TE Policy segment weight settings.
      type: object
      properties:
        enable:
          x-device-pattern:
            description: >-
              Enable the weight settings
            format: boolean 
            default: false
        weight:
          x-device-pattern:
            description: >-
              The weight associated with a given path.
            format: integer
            default: 0
