components:
  schemas:
    RTPProfile:
      properties:
        encryptionmode:
          $ref: ../rtpencryptionmode/rtpencryptionmode.yaml#/components/schemas/RTPEncryptionMode
          x-field-uid: 1
        mosmode:
          $ref: ../mosmode/mosmode.yaml#/components/schemas/MosMode
          x-field-uid: 2
        profileid:
          description: 'The ID of the RTP profile (default: VoiceProfile).'
          type: string
          x-field-uid: 3
      required:
      - profileid
      - mosmode
      - encryptionmode
