components:
  schemas:
    Device.BgpPreferenceSubTlv:
      description: >-
        Container for BGP preference sub tlv settings.
      type: object
      properties:
        enable:
          x-device-pattern:
            format: boolean
            default: false
        preference:
          x-device-pattern:
            format: integer
            default: 0
