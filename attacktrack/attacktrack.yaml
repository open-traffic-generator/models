components:
  schemas:
    AttackTrack:
      properties:
        actions:
          items:
            $ref: ../attackaction/attackaction.yaml#/components/schemas/AttackAction
          type: array
          x-field-uid: 1
        add_actions:
          items:
            format: byte
            type: string
          type: array
          x-field-uid: 2
      type: object
