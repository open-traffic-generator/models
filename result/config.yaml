openapi: 3.1.0

info:
  title: Config results models
  version: ^0.0.0

components:
  schemas:
    Result.ConfigResponse:
      description: >-
        The config result request to the traffic generator.
        Returns the configuration from the traffic generator.
      type: object
      properties:
        request_state:
          $ref: './request.yaml#/components/schemas/Result.RequestState'
        config:
          $ref: '../config/config.yaml#/components/schemas/Config'


      
