components:
  schemas:
    Traffic.Server:
      description: >-
        Traffic .
      type: object
      properties:
        endpoints:
          description: >-
            EndPoint configuration for one or more emulated network interfaces.
          type: array
          items:
            $ref: './endpoints.yaml#/components/schemas/Traffic.Endpoints'
          x-field-uid: 1
        protocol:
          description: >-
            TCP interface name
          type: string
          x-constraint:
          - '/components/schemas/App.Tcp/properties/name'
          x-field-uid: 2