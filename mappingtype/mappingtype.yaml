components:
  schemas:
    MappingType:
      description: 'Use this property to switch between different meshing types (default:
        FULL_MESH).'
      type: string
      x-enum:
        FULL_MESH:
          x-field-uid: 1
        VLAN_TO_IP_CUSTOM_MESH:
          x-field-uid: 2
