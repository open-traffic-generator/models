components:
  schemas:
    Track:
      properties:
        actions:
          items:
            $ref: ../action/action.yaml#/components/schemas/Action
          type: array
          x-field-uid: 1
        add_actions:
          items:
            format: byte
            type: string
          type: array
          x-field-uid: 2
      type: object
